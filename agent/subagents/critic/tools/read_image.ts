// biome-ignore lint/performance/noBarrelFile: eve discovers child tools by path; the critic reads cited screenshots itself because a child cannot delegate to the vision station.
export { default } from "../../vision/tools/read_image.js";
